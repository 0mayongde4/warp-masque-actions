{
  "name": "opera-masque-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node test/auth.test.mjs && node test/route.test.mjs"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
